# from App.config import db

# class Game(db.Model):#TODO Make this an abstract class or future games to inherit from
#     id = db.Column(db.String(60),primary_key=True)
#     questions = db.relationship("Question")
